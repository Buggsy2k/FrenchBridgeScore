import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.buggsy2k.frenchbridge',
  appName: 'French Bridge Scores',
  webDir: 'dist'
};

export default config;
